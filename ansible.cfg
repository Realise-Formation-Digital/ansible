[defaults]
# Do not create .retry files on failed playbook runs
retry_files_enabled = False

# Suppress Python interpreter version warnings
interpreter_python = auto_silent

# Collection search paths
collections_paths = ~/.ansible/collections:/usr/share/ansible/collections

[ssh_connection]
# Speed up SSH by reusing connections across tasks
pipelining = True
