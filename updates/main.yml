---
# Playbook for periodic host updates.
# Run with: ansible-playbook updates/main.yml -i <inventory>
#
# Also used by Semaphore UI for automated updates via the 'maintenance' user.

- hosts: all
  become: true

  tasks:
    - import_tasks: ./updates.yml
